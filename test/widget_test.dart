import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads successfully', (WidgetTester tester) async {
    // Placeholder test - Hive must be mocked/initialized for full widget tests.
    expect(true, isTrue);
  });
}
