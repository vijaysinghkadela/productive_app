name: Deploy
on:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - run: npm ci
      - run: npm run build
      - name: Check for Firebase token
        id: check-token
        run: |
          if [ -z "${{ secrets.FIREBASE_TOKEN }}" ]; then
            echo "has_token=false" >> $GITHUB_OUTPUT
            echo "⚠️ FIREBASE_TOKEN secret not configured. Skipping deploy."
          else
            echo "has_token=true" >> $GITHUB_OUTPUT
          fi
      - name: Deploy to staging
        if: steps.check-token.outputs.has_token == 'true'
        run: npx firebase-tools deploy --only functions,firestore,storage --project focusguard-staging --force
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      - name: Post-deploy health check
        if: steps.check-token.outputs.has_token == 'true'
        run: |
          sleep 10
          curl -f "https://us-central1-focusguard-staging.cloudfunctions.net/api/health" || echo "Health check failed (expected if first deploy)"

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [deploy-staging]
    environment: production
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - run: npm ci
      - run: npm run build
      - name: Deploy to production
        run: npx firebase-tools deploy --only functions,firestore,storage --project focusguard-prod --force
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN_PROD }}
      - name: Post-deploy health check
        run: |
          sleep 15
          curl -f "https://us-central1-focusguard-prod.cloudfunctions.net/api/health" || exit 1
      - name: Notify success
        if: success()
        run: echo "🚀 Production deploy successful for ${{ github.ref_name }}"
      - name: Notify failure
        if: failure()
        run: echo "❌ Production deploy FAILED for ${{ github.ref_name }}"
